repositories:
  - name: grafana
    url: https://grafana.github.io/helm-charts
releases:
  - name: loki
    namespace: monitor
    chart: grafana/loki
    version: 5.42.2
    disableValidationOnInstall: true
    values:
      - values.yaml
environments:
  dev: {}
  ci: {}
