repositories:
  - name: jaegertracing
    url: https://jaegertracing.github.io/helm-charts
releases:
  - name: jaeger
    namespace: monitor
    chart: jaegertracing/jaeger
    version: 3.4.1
    values:
      - values.yaml
environments:
  dev: {}
  ci: {}
